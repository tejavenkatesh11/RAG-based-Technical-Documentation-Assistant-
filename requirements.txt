# Pinned to the versions verified working together in the conda `main` env.

# --- API ---
fastapi>=0.135
uvicorn[standard]>=0.41
pydantic>=2.12
pydantic-settings>=2.12
python-multipart>=0.0.20
python-dotenv>=1.0

# --- LangChain / LangGraph stack ---
langgraph>=1.2
langchain>=1.3
langchain-core>=1.4
langchain-community>=0.4
langchain-text-splitters>=1.1
langchain-chroma>=1.1

# --- LLM (native Groq SDK; we do NOT use langchain-groq due to version pinning) ---
groq>=1.1

# --- Vector store + embeddings (local, no API key needed) ---
chromadb>=1.5
sentence-transformers>=5.2

# --- Postgres ---
sqlalchemy>=2.0
psycopg2-binary>=2.9

# --- Document ingestion ---
beautifulsoup4>=4.14
soupsieve>=2.8
lxml>=6.1
httpx>=0.28
markdownify>=1.2

# --- Utilities ---
tenacity>=8.5

# --- Optional UI ---
streamlit>=1.57
